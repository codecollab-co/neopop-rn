module.exports = {
  // No native dependencies to auto-link
  // All native deps (Skia, Reanimated, RNGH) are peer dependencies
  // managed by the consumer's Expo / RN project
  dependencies: {},
};

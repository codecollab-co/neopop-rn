/**
 * Utility barrel — re-exports all public utility functions.
 *
 * - `colorUtils`: hexToRGBA, getHorizontalShadow, getVerticalShadow, deriveEdgeColor, getLuminance
 * - `helpers`:    isEmpty, isObject, mergeDeep, currencyFormatter, generateTextStyle
 * - `haptics`:    triggerHaptic
 */
export * from './colorUtils';
export * from './helpers';
export * from './haptics';
